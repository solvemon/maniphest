{
  "name": "maniphest",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --force",
    "test": "npm run build && npm run lint && npm test --workspaces",
    "lint": "eslint packages/sim"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "@types/node": "^24",
    "eslint": "^10",
    "@babel/core": "^8",
    "@babel/eslint-parser": "^8",
    "@babel/preset-typescript": "^8"
  }
}
